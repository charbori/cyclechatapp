alter table `user` add constraint primary key(`idx`);
alter table `shop` add constraint primary key(`idx`);
alter table `product` add constraint primary key(`idx`);
alter table `cart` add constraint primary key(`idx`);
alter table `cart_manager` add constraint primary key(`idx`);
alter table `order` add constraint primary key(`idx`);
alter table `delivery` add constraint primary key(`idx`);
alter table `refund` add constraint primary key(`idx`);
